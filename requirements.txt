flask
flask-cors
gunicorn
yfinance>=0.2.50,<1.0.0
pandas
numpy
requests
psycopg2-binary
python-dotenv
urllib3

# borsapy: hem TradingView WebSocket live tick (realtime_prices.py) hem _fetch_borsapy_quick/_hist
# için kritik. Try/except ile sarmali — kurulum başarısızsa yfinance/Yahoo HTTP fallback'e düşer
# ama TV WebSocket tamamen devre dışı kalır (live tick akışı durur, regime/auto-trader 5 dk hist baseline'a dönüş).
borsapy
