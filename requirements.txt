flask
flask-cors
gunicorn
yfinance>=0.2.50,<1.0.0
pandas
numpy
requests
psycopg2-binary
python-dotenv
urllib3

# Opsiyonel - borsapy birincil veri kaynagi olarak kullaniliyor (try/except ile sarmali,
# yoksa yfinance/Yahoo HTTP fallback'e dusuyor). Pip'te yoksa veya kurulum sorun cikariyorsa
# yorum satiri olarak birakilabilir.
# borsapy
